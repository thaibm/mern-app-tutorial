import { createTheme, Theme } from '@material-ui/core'

export const theme: Theme = createTheme({
  palette: {
    primary: {
      main: '#013440',
      contrastText: '#ffffff',
    }
  }
});
